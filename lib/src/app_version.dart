/// גרסת חנות התוספים העצמאית — **מספר שלם אחד**: 1, 2, 3…
///
/// **חייבת להסכים עם `version` שב-`pubspec.yaml`**, שנושא `.0.0` נוסף כי pub
/// דורש שלושה חלקים. משם `build_stub.ps1` צורב אותה גם למרקר `.ready` (מה
/// שמכריע אם ה-stub מחלץ מחדש) וגם למשאב הגרסה של ה-exe.
///
/// אין להציב ידנית: `tool/set_version.sh <N>` מציב את שני הקבצים יחד,
/// ו-`test/app_version_test.dart` נכשל אם הם נפרדו. הקובץ כאן מחזיק את
/// **הגרסה שפורסמה לאחרונה**, וה-CI מקדם אותה בפרסום הבא — `0` פירושו
/// "עוד לא פורסם דבר", ולכן בנייה מקומית לפני ה-release הראשון מדווחת 0.
const String appVersion = '1';
