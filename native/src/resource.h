// מזהי המשאבים שב-exe. חייבים להתאים ל-app.rc.
#pragma once

// חבילת קובצי הממשק, כפי ש-native/tools/pack_web.ps1 אורז אותה.
#define IDR_WEB_BUNDLE 100

// האייקון של התוכנה. **הראשון בקובץ ה-rc** — ווינדוס בוחרת את האייקון
// בעל המזהה הנמוך ביותר כאייקון של קובץ ההרצה ב-Explorer, ולכן זה מה
// שמופיע גם בשורת המשימות וגם על הקובץ עצמו.
#define IDI_APP_ICON 101
